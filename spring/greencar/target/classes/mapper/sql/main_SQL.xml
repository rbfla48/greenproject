<?xml version="1.0" encoding="UTF-8"?> 
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<!-- 프로젝트에서는 기본적으로 여러개의 <mapper>를 가지기 때문에 중복되는 이름을 가진 SQL이 존재할 수 있다. 
	따라서 각 <mapper>마다 namespace 속성을 이용하여 <mapper>간 유일성을 보장해야 한다.-->

<mapper namespace="sample">
		<!-- SELECT문임을 명시하고, id정의(selectBordList) 
		parameterType="hashmap" ==> 해당쿼리사용시 필요한 변수가 hashmap이다
		resultType="hashmap" ==> 해당쿼리의 결과값이 hashmap에 담겨서 반환된다-->
		<select id="selectBoardList" parameterType="hashmap" resultType="hashmap">
			<![CDATA[SELECT brand_name FROM brand]]>
		</select> 	
</mapper>



