<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="productMapper">
	<!--  상품 삽입 -->
	<insert id="insertProduct" parameterType="com.javateam.main.vo.product.ProductVO">
		INSERT INTO board_product (
			product_no,
			product_name,
			product_size,
			product_type,
			product_price,
			product_efficient,
			product_fuel,
			product_code
			)
		VALUES( 
			board_product_SEQ.NEXTVAL
			, #{productNo}
			, #{productName}
			, #{productSize}
			, #{productType}
			, #{productPrice}
			, #{productEfficient}
			, #{productFuel}
			, #{product_code})
	</insert>	

	<!-- 상품 목록 조회 -->
	<select id="listProduct" resultType="com.javateam.main.vo.product.ProductVO">
		select product_no,
			product_name,
			product_size,
			product_type,
			product_price,
			product_efficient,
			product_fuel,
			product_code
		from board_product
		order by product_no DESC
	</select>
	
	<!-- 상품 조회 -->
	<select id="readProduct" parameterType="int" resultType="com.javateam.main.vo.product.ProductVO">
		select product_no,
			product_name,
			product_size,
			product_type,
			product_price,
			product_efficient,
			product_fuel,
			product_code
		from board_product
		where product_no = #{productNo}
	</select>
	
	<!--  상품 수정 -->
	<update id="updateProduct" parameterType="com.javateam.main.vo.product.ProductVO">
		update board_product
			set product_name = #{productName},
				product_size = #{productSize},
				product_type = #{productType},
				product_price = #{productPrice},
				product_efficient = #{productEfficient},
				product_fuel = #{productFuel}
		where product_no = #{productNo}
	</update>
	
</mapper>